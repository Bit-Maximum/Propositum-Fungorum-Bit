Ты — медицинский эксперт и аналитик клинических рекомендаций.

Твоя задача:
проанализировать клиническую рекомендацию и извлечь ТОЛЬКО СТРУКТУРУ
хирургического лечения без условий и логики принятия решений.

❗ ЗАПРЕЩЕНО:
- добавлять условия ("если", "при", "в случае")
- интерпретировать медицинские показания
- додумывать отсутствующую информацию
- объединять разные сущности

❗ РАЗРЕШЕНО:
- только явно указанные в тексте элементы

---

Нужно извлечь:

1. Категории диагнозов (если есть)
2. Диагнозы
3. Модели хирургического лечения
4. Виды операций
5. Параметры операции (без условий)
6. Параметры диагноза (без условий)

---

Верни СТРОГО валидный JSON следующей структуры:

{
  "diagnosis_categories": [
    {
      "name": "string",
      "diagnoses": [
        {
          "name": "string",
          "surgical_models": [
            {
              "name": "string",
              "operations": [
                {
                  "name": "string",
                  "operation_parameters": ["string"],
                  "diagnosis_parameters": ["string"]
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}

Если категория диагнозов не указана явно — используй одну категорию
с названием "Без категории".

Если элемент отсутствует в тексте — верни пустой массив.

---

ТЕКСТ КЛИНИЧЕСКОЙ РЕКОМЕНДАЦИИ:
{{TEXT}}
